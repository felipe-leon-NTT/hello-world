name: 'Triggering - Run a workflow when a pull request is opened'

on:
  pull_request:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World! I ran because a pull request was opened"
      - name: Checkout code
        uses: actions/checkout@v2
      - run: ls -l
